// SPA / PWA mode: render entirely on the client. No server-side rendering and
// no prerendering — the static adapter emits index.html as a fallback shell.
export const ssr = false;
export const prerender = false;
